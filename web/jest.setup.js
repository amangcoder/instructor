import '@testing-library/jest-dom'

/**
 * Jest Setup File
 * Configures the test environment with @testing-library/jest-dom matchers
 *
 * To run tests:
 * 1. Install dependencies: npm install --save-dev jest @testing-library/react @testing-library/jest-dom @testing-library/user-event jest-environment-jsdom
 * 2. Run tests: npm test
 * 3. Run tests with coverage: npm test -- --coverage
 */
