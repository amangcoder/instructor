version: 1
frontend:
  phases:
    preBuild:
      commands:
        - export NEXT_TELEMETRY_DISABLED=1
        - npm install -g pnpm@latest --quiet
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
